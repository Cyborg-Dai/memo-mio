

"scripts": {
    "start": "http-server -c -1", //Elijo el nombre del comando "start". Ejecuta el comando "http-server -c -1", para iniciar el servidor
    "cypress-run": "cypress run",
    "cypress-dev": "cypress open",
    "test": "run-p start cypress-run",
    "test-dev": "run-p start cypress-dev"
}

  "dependencies": {
    "bootstrap": "^5.3.3"
  }

